version: "2"
sql:
  - engine: postgresql
    schema: db/migrations/000001_initial_schema.up.sql
    queries: db/queries
    gen:
      go:
        package: db
        out: apps/api/internal/storage/db
        sql_package: database/sql
        emit_json_tags: true
        emit_empty_slices: true
        rename:
          nich: Niche
