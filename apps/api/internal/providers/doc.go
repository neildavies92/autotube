// Package providers defines integration contracts for external systems.
//
// The package intentionally contains interfaces and shared request/response
// shapes only. Concrete provider clients belong in later tickets once each
// workflow knows which capabilities it needs.
package providers
